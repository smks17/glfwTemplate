# glfwTemplate
my glfw template

## build and run
### build in Windows

    mkdir build
    cd build
    cmake ..
    cmake -build .

### build in Unix

    mkdir build
    cd build
    cmake ..
    make

### run
for the run, in the build directory you should just run 'glfwTest.exe' in Windows and 'glfwTest' in Unix.